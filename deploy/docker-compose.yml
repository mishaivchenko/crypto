services:
    app:
        image: funding:arm64
        container_name: funding-app
        env_file:
            - .env
        ports:
            - "${SERVER_PORT:-8090}:8090"
        volumes:
            - ./data:/data
        restart: unless-stopped
