# B-2 — Engine credentials не подхватываются через UI monitor

**Тип:** Bug  
**Приоритет:** medium  
**Статус:** open

Engine credentials не подхватываются через UI monitor — нужно отдельно выставлять `ENGINE_CREDENTIALS_*` env vars в engine-контейнере. Неочевидно для оператора.
