# B-3 — ENGINE_LIVE_ORDER_ENABLED и KILL_SWITCH не видны в UI

**Тип:** Bug
**Приоритет:** low
**Статус:** open

`ENGINE_LIVE_ORDER_ENABLED` и `ENGINE_KILL_SWITCH_ENABLED` не видны в UI — оператор не может понять почему LAB Run Once падает без чтения логов.
